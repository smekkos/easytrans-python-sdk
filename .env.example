# EasyTrans SDK — integration test credentials template
# Copy this file to .env and fill in your values.
# .env is git-ignored; .env.example is committed as documentation.

# ── Connection ────────────────────────────────────────────────────────────────
EASYTRANS_SERVER=mytrans.nl            # hostname only, no https://
EASYTRANS_ENV=demo                     # environment path segment
EASYTRANS_USERNAME=your_username
EASYTRANS_PASSWORD=your_password

# ── JSON Import API tests ─────────────────────────────────────────────────────
# A product number valid in your environment.
# Find it in the EasyTrans Customer Portal → Products/Services.
EASYTRANS_TEST_PRODUCTNO=2

# A customer number to attach to every order.
# Required only for branch accounts (leave blank for direct-customer accounts).
EASYTRANS_TEST_CUSTOMERNO=

# ── REST API tests — known entity IDs ─────────────────────────────────────────
# Each variable is optional.  Tests that require a specific ID are automatically
# skipped (not failed) when the corresponding variable is not set.
#
# Tip: Run ``client.get_orders().data[0].id`` in a Python shell to find a valid
# order number for your environment.

# An order number that already exists in your environment.
EASYTRANS_REST_KNOWN_ORDER_NO=

# A product number (use the same value as EASYTRANS_TEST_PRODUCTNO if available).
EASYTRANS_REST_KNOWN_PRODUCT_NO=

# A substatus number visible in EasyTrans → Settings → Substatuses.
EASYTRANS_REST_KNOWN_SUBSTATUS_NO=

# A package type number visible in EasyTrans → Settings → Package types.
EASYTRANS_REST_KNOWN_PACKAGE_TYPE_NO=

# A vehicle type number visible in EasyTrans → Settings → Vehicle types.
EASYTRANS_REST_KNOWN_VEHICLE_TYPE_NO=

# A customer number (branch accounts only).
EASYTRANS_REST_KNOWN_CUSTOMER_NO=

# A carrier number (branch accounts only).
EASYTRANS_REST_KNOWN_CARRIER_NO=

# A fleet number (branch accounts only).
EASYTRANS_REST_KNOWN_FLEET_NO=

# An internal invoice ID (not the invoice number printed on PDF).
# Find it in the EasyTrans portal or via client.get_invoices().data[0].invoice_id.
EASYTRANS_REST_KNOWN_INVOICE_ID=
